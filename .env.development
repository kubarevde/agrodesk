# Empty = use Vite proxy (/api → localhost:8000). Avoids CORS localhost vs 127.0.0.1.
# For direct API calls set e.g. VITE_API_URL=http://localhost:8000
VITE_API_URL=
VITE_APP_NAME=АгроДеск
VITE_USE_MOCKS=false
